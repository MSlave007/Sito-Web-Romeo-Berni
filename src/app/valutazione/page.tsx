import ContactPage from "../contatti/page";

// Check if we can reuse the same page or if we need slight variations. 
// For now reusing the Contact Page layout/logic is fine as per spec they target the same form.
export default ContactPage;
